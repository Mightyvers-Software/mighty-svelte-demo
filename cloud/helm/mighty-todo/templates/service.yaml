apiVersion: v1
kind: Service
metadata:
  name: "{{ include "mighty-todo.fullname" . }}"

spec:
  selector:
    app: "{{ include "mighty-todo.name" . }}"

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000

  type: ClusterIP